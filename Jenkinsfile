pipeline {
	agent any
	
	stages {
		stage("build"){
			steps{
				echo 'built maven app'
			}
		}
		stage("test"){
			steps{
				echo 'testing the app'
			}
		}
		stage("deploy"){
			steps{
				echo 'deploying the app'
			}
		}
	}
}
